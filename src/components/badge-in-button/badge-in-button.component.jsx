import React from 'react';
import { Container } from './badge-in-button.styles';

const BadgeInButton = ({ children }) => {
  return <Container>{children}</Container>;
};

export default BadgeInButton;
